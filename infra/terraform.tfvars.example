# Copy to terraform.tfvars and fill in your values.
# Do NOT commit terraform.tfvars — it is git-ignored.

aws_region  = "eu-west-2"
bucket_name = "signal-site-yourname"   # must be globally unique
environment = "production"
